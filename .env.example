# Copy this file to .env.local and fill in your actual values

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# 100ms (Frontend uses only access key; secret must stay server-side)
VITE_HMS_APP_ACCESS_KEY=your_100ms_app_access_key

# Supabase Edge Function (server-side)
# Set these in Supabase project (NOT in frontend .env):
# HMS_APP_ACCESS_KEY=your_100ms_app_access_key
# HMS_APP_SECRET=your_100ms_app_secret

# Instructions:
# 1. Go to your Supabase project settings
# 2. Navigate to API settings
# 3. Copy the Project URL and Project API keys
# 4. Replace the values above with your actual keys
# 5. The ANON key is safe to use in frontend applications
# 6. The SERVICE_ROLE key should be kept secret
# 7. DO NOT commit the .env.local file to version control
