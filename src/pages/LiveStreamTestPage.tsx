import { LiveStream } from "@/components/live-stream";

const LiveStreamTestPage = () => {
  return (
    <div className="w-full h-screen">
      <LiveStream />
    </div>
  );
};

export default LiveStreamTestPage;
